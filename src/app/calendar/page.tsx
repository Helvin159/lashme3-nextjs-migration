import React from 'react';
import Section from '@/components/Section';
import Container from '@/components/Container';

const Calendar = () => {
	return (
		<Section>
			<Container>Calendar Page</Container>
		</Section>
	);
};

export default Calendar;
